@model IEnumerable<TallerFrameWork.Models.producto>

<h2>Cargar Imagen Producto</h2>

@using (Html.BeginForm("Cargar Imagen", "Producto_Imagen", FormMethod.Post, new { enctype = "multipart/formdata" }))
{
    @Html.AntiForgeryToken()
    <div class="form-group">
        <label class="control-label col-md-2">Imagen </label>
        <div class="col-md-10">
            <input type="file" name="fileForm" class="form-control" required />
        </div>
    </div>

    <div class="form-group">
        <label class="control-label col-md-2">Producto</label>
        <div class="col-md-10">
            <input type="number" name="producto" class="form-control" />
        </div>
    </div>

    @*<div class="form-group">
        <label class="control-label col-md-2">Producto</label>
        <select class="form-control" id="id_producto" name="id_producto">
            @foreach (var item in Model)
            {
                <option value="@item.id">@item.nombre</option>
            }
        </select>
    </div>*@

    <div class="form-group">
        <div class="col-md-10">
            <input type="submit" value="Enviar" />
        </div>
    </div>
}